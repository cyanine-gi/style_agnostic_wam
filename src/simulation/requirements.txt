# 合并环境 requirements：Isaac Sim 仿真 + WAM 训练同进程（单进程调试/在线更新参数用）
# 2026-09-13 起意，基于实测可用的 env_isaaclab（torch 2.7.0+cu128 基）+
# style_agnostic_wam 训练栈合并；两原环境均已钉死，本文件只用于新建环境。
#
# 用法（新建环境，不要往现有环境里装）：
#   conda create -n <新环境名> python=3.11 -y && conda activate <新环境名>
#   pip install -r src/simulation/requirements.txt \
#       --extra-index-url https://download.pytorch.org/whl/cu128 \
#       -i https://mirrors.aliyun.com/pypi/simple/
#   pip install -e /mnt/nvme4t/IsaacLab/source/isaaclab \
#                -e /mnt/nvme4t/IsaacLab/source/isaaclab_assets \
#                -e /mnt/nvme4t/IsaacLab/source/isaaclab_tasks \
#                -e /mnt/nvme4t/IsaacLab/source/isaaclab_rl \
#                -e /mnt/nvme4t/IsaacLab/source/isaaclab_mimic   # Isaac Lab v2.3.2 源码
#
# 注意：torch 必须先于/随本文件装 cu128 版（Isaac Sim 5.1 硬性要求 2.7.0，
# 绝对禁止升到 2.9）；装完任何新包后跑 pip check 并对照下方钉版清单。

# ---- DL 基座（Isaac Sim 5.1 强制钉版，顺序/版本均不可动）----
torch==2.7.0
torchvision==0.22.0

# ---- Isaac Sim 5.1（pip 全家桶）----
isaacsim[all,extscache]==5.1.0

# ---- Isaac Sim 5.1 强制钉版（装新包时禁止被顶掉）----
packaging==23.0
psutil==5.9.8
click==8.1.7
typing_extensions==4.12.2

# ---- RL 库（与实测可用的 env_isaaclab 对齐）----
gymnasium==1.2.1
rsl-rl-lib==3.1.2
skrl==2.1.0
rl_games==1.6.1
stable-baselines3==2.7.1      # 上限 <2.8（Isaac Lab 兼容）
wandb==0.22.3                 # 上限 <0.23
onnx==1.18.0                  # 上限 <1.19
ipython==8.39.0               # 上限 <9
starlette==0.45.3             # 上限 <0.46
# flatdict 若报构建错误：pip install flatdict==4.0.1 --no-build-isolation
# （需 setuptools<81，实测 setuptools==80.10.2 可用）

# ---- WAM 训练栈（与 style_agnostic_wam/requirements.txt 对齐，torch 除外）----
numpy>=1.26,<2.0
scipy>=1.10
h5py>=3.8
matplotlib>=3.7
opencv-python>=4.8            # 注意：Isaac 侧原为 headless 变体，合并环境用普通版
                              #（我们的可视化脚本要 GUI）；若仿真 headless 报错再议
tqdm>=4.65
modelscope
transformers==4.57.6
accelerate==1.10.1
qwen-vl-utils==0.0.14
timm==1.0.20
einops==0.8.2
PyYAML==6.0.3
tensorboard==2.21.0
pytest==9.1.1
